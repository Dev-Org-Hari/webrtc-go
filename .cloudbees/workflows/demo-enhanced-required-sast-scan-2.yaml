  name: demo-enhanced-required-sast-scan-noscan
  apiVersion: automation.cloudbees.io/v1alpha1
  kind: workflow
  on:
    workflow_dispatch: {}
  permissions:
    id-token: write
  jobs:     
    build: 
      steps:
        - name: Build something
          uses: docker://alpine:latest
          shell: sh
          run: echo "Building..."


